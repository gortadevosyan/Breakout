module Scenes.LogoEnd.Common exposing (Model, nullModel, initModel)

{-| Common module

This module is generated by Messenger, don't modify this.

@docs Model, nullModel, initModel

-}

import Lib.Env.Env exposing (Env, addCommonData)
import Lib.Scene.Base exposing (LayerPacker, SceneInitData(..))
import Scenes.LogoEnd.Background.Export as Background
import Scenes.LogoEnd.Background.Global as BackgroundG
import Scenes.LogoEnd.LayerBase exposing (CommonData, nullCommonData)
import Scenes.LogoEnd.LayerSettings exposing (LayerT)
import Scenes.LogoEnd.SceneInit exposing (initCommonData, nullLogoEndInit)


{-| Model
-}
type alias Model =
    LayerPacker CommonData LayerT


{-| nullModel
-}
nullModel : Model
nullModel =
    { commonData = nullCommonData
    , layers = []
    }


{-| Initialize the model
-}
initModel : Env -> SceneInitData -> Model
initModel env init =
    let
        layerInitData =
            case init of
                LogoEndInitData x ->
                    x

                _ ->
                    nullLogoEndInit
    in
    { commonData = initCommonData env layerInitData
    , layers =
        [ BackgroundG.getLayerT <| Background.initLayer (addCommonData nullCommonData env) layerInitData
        ]
    }
