module Scenes.Story.Common exposing (Model, nullModel, initModel)

{-| Common module

This module is generated by Messenger, don't modify this.

@docs Model, nullModel, initModel

-}

import Lib.Env.Env exposing (Env, addCommonData)
import Lib.Scene.Base exposing (LayerPacker, SceneInitData(..))
import Scenes.Story.Background.Export as Background
import Scenes.Story.Background.Global as BackgroundG
import Scenes.Story.LayerBase exposing (CommonData, nullCommonData)
import Scenes.Story.LayerSettings exposing (LayerT)
import Scenes.Story.Room.Export as Room
import Scenes.Story.Room.Global as RoomG
import Scenes.Story.SceneInit exposing (initCommonData, nullStoryInit)


{-| Model
-}
type alias Model =
    LayerPacker CommonData LayerT


{-| nullModel
-}
nullModel : Model
nullModel =
    { commonData = nullCommonData
    , layers = []
    }


{-| Initialize the model
-}
initModel : Env -> SceneInitData -> Model
initModel env init =
    let
        layerInitData =
            case init of
                StoryInitData x ->
                    x

                _ ->
                    nullStoryInit
    in
    { commonData = initCommonData env layerInitData
    , layers =
        [ BackgroundG.getLayerT <| Background.initLayer (addCommonData nullCommonData env) layerInitData
        , RoomG.getLayerT <| Room.initLayer (addCommonData nullCommonData env) layerInitData
        ]
    }
